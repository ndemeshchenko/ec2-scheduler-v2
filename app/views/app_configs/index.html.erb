<div class="row">

<div class="col-md-6">
  <h1>AWS Scanner events</h1>
    <% @app_configs.first.events.split(',').each do |event| %>
          <span class="label label-default">
            <%= event %>
          </span><br>
      <% end %>
  <hr>
  <td><%= link_to 'Edit', edit_app_config_path(@app_configs.first) %></td>

  <%#= link_to 'New App config', new_app_config_path %>
</div>
<div class="col-md-3"></div>
</div>
