<%= simple_form_for(@app_config) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.text_area :events.to_s, cols: '60', rows: '10' %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
