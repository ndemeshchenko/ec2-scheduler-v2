<%= simple_form_for(@app_config) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :events %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
